# Python program to find the length of a set

myset = {"atharv", "baban", "jerry", "maya"} # Create a set

length = len(myset) # Find length using len()

print("The set is:", myset)
print("Length of the set:", length)
